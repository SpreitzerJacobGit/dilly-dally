node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
verticals/*/db/*.db*
*.local
.DS_Store

# Local scratch for prepared map/routing inputs — multi-GB, never committed.
.data-src/
.tiles/

# FCC Broadband Data Collection API credentials for the coverage refresh.
# Lives on the van server only; the app itself never needs it.
.fcc-token

